# Third-party notices

Agent Annotations depends on `react-grab@0.1.50` as its sole generic element
inspection engine and on `magic-string` (used by the Vite plugin for source
mapping). React, React DOM, and Vite are peer dependencies supplied by the
host application. Their respective licenses apply to those packages.

The complete dependency inventory and license texts for an installation are
available from the package manager lockfile and installed package metadata.
