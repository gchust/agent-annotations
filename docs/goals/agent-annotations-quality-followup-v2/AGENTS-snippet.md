## Agent Annotations follow-up Goals

For work under `docs/goals/agent-annotations-quality-followup-v2/`:

- Read `00-shared-contract.md` and only the currently assigned numbered Goal.
- Inspect the actual HEAD before editing.
- Complete and verify one Goal before starting the next.
- Keep Progress, Surprises & Discoveries, Decision Log, and Outcomes current.
- Every PASS requires an actually run command or browser/package/CI artifact.
- Do not restore MCP, `verify`, NocoBase coupling, old browser-state/CLI
  compatibility, fuzzy marker fallback, React Fiber access, or source guessing.
- Do not claim remote CI or platform coverage that did not run.
