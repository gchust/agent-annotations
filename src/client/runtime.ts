// Thin compatibility entry: the full implementation lives in the decomposed
// src/client/runtime/ modules; mount.ts owns the orchestration.
export { mountAgentAnnotations } from "./runtime/mount.js";
